# Core dependencies
streamlit==1.34.0
streamlit_modal==0.1.2
python-dotenv==1.0.1

# Data processing
pandas==2.2.2
numpy>=1.26.0,<2.0.0

# LangChain (pinned for compatibility)
langchain==0.1.17
langchain-community==0.0.36
langchain-core==0.1.50
langchain-openai==0.1.6
langchain-text-splitters==0.0.1

# OpenAI
openai>=1.0.0,<2.0.0

# ML/AI packages (large - install takes 30-60 min on first deploy)
# These are lazy-loaded, but must be installed
sentence-transformers==2.2.2
faiss-cpu>=1.8.0

# Pydantic (compatibility with LangChain)
pydantic>=2.0.0,<3.0.0

# Utilities
httpx==0.27.0
huggingface-hub==0.25.0
pypdf==5.0.0